/* eslint-disable no-empty */
/* eslint-disable no-trailing-spaces */
/* eslint-disable no-unused-vars */

const greedyBfs = (startNode, endNode) => {
    let priorityQueue = [];

    priorityQueue.push(startNode);

    // while (priorityQueue.length > 0) {

    // }

};

export default greedyBfs;